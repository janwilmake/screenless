{
  "name": "voxcall",
  "version": "0.1.0",
  "description": "Call yourself with an AI voice agent from the command line, get the transcript back",
  "type": "module",
  "bin": { "voxcall": "./dist/index.js" },
  "files": ["dist"],
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
